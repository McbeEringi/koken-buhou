# JSで ファイル生成 ZIP編

22 McbeEringi

// <https://qiita.com/McbeEringi/items/5acc1f940ff7504f7e16>の修正兼詳細版

## 動機

ブラウザで何かしらのデータを生成した場合、これをユーザーに保存させる手段として`<a download>`を用いたダウンロードが一般的である

しかしこの方法を用いる場合、一度に保存できるファイルは一つに限られる

これを解決する手段として、ZIPやtarなどの複数ファイルを一つのファイルにまとめる機能を持つファイル形式がよく用いられる

ブラウザ上でこの動作を実現するライブラリは複数存在するが、動作速度やライブラリサイズ等において満足いくものを見つけられなかった

## 目標

- 複数ファイルの一括ダウンロード
  - 📦 汎用 シンプル
  - 🪶 軽量 ( 数kB以内 )
  - ⚡ 爆速 ( 「見逃しちゃうね」 )

## ファイルフォーマットの選定

「複数ファイルを一つにまとめたファイル」はアーカイブファイルと呼ばれる

「元のファイルと同じ内容を表すより容量の小さいファイル」は圧縮ファイルと呼ばれる

これらは全く独立した概念である

- アーカイブのみ
  - tar  
    > tape_archive tarball 紙テープに穴開けてた時代から使われている
- 圧縮のみ
  - gzip  
    > .tar.gzでわりとよくみる
  - bzip2  
    > .tar.bz2でたまにみる
  - xz  
    > .tar.xz 逢えたらラッキー
- アーカイブ及び圧縮
  - 7z  
    > 圧縮解凍ソフト「7-Zip」といえばこれ 高圧縮率
  - LHA  
    > 純日本産
  - ZIP  
    > いつもの 元は圧縮解凍ソフト「PKZIP」の形式  
    > 様々な端末がデフォルトで対応している

今回の目標はアーカイブファイルの作成に言い換えることができる

以下の二つの形式を検討した

- tar
  - メリット
    - 広く普及している
    - アーカイブ機能のみであるため読み込みもできそう
  - デメリット
    - 歴史が長いので方言が存在
    - スマホで開きづらい
    - ダウンロードボタン押してtarがきたらなんだこれはになる
- ZIP
  - メリット
    - 広く普及している
    - 正しくファイルが入手できたという安心感がある
    - iOSですら標準ソフトで開ける
  - デメリット
    - 圧縮されている場合は読み込みが辛そう

結果今回はZIP形式を用いることにした

当然の帰結

## ZIPファイルについて

Wikipediaにて詳しく解説されているので主にこれを参考にした

ZIPファイルは以下のような構造をしている

```text

|**|~~~~~~~~|**|~~~~~~~~|**|~~~~~~~~|***|***|***|;|
-ﾛｰｶﾙﾌｧｲﾙｴﾝﾄﾘ-                       --ｾﾝﾄﾗﾙﾃﾞｨﾚｸﾄﾘ-- 
 ﾍｯﾀﾞ                                ﾚｺｰﾄﾞ        終端
    ﾃﾞｰﾀ

```

- ZIPファイル
  - ローカルファイルエントリ
    - ローカルファイルヘッダ
      > - `PK\3\4` から始まる
    - 実データ
      > - 基本的に圧縮されている
      >   - 圧縮しないこともできる ( 今回はこれ )
      > - 圧縮は各ファイル毎に行われる
  - ローカルファイルエントリ
  - ...
  - ローカルファイルエントリ
  - セントラルディレクトリ
    - セントラルディレクトリレコード
      > - `PK\1\2` から始まる  
      > - ローカルファイルヘッダの拡張  
      > - ローカルファイルエントリと一対一対応
      >   - ローカルファイルヘッダの位置が記録されている
    - セントラルディレクトリレコード
    - ...
    - セントラルディレクトリレコード
    - セントラルディレクトリ終端レコード ( EOCD )
      > - `PK\5\6` から始まる  
      > - セントラルディレクトリの位置が記録されている

ファイル読み込みは以下の順であると考えられる

- セントラルディレクトリ終端レコードを検索
- セントラルディレクトリ終端レコードからセントラルディレクトリの先頭へ移動
- 各セントラルディレクトリレコードから各ローカルファイルエントリの先頭へ移動
- 実データを処理

### 各部詳細

ローカルファイルヘッダとセントラルディレクトリレコードは、ほとんどが共通の内容である

ディレクトリは、解凍時のZIPファイルの位置を基準とした相対パスをファイル名として記述することで表現される

全てのマルチバイトな数値は、リトルエンディアンで格納される

今回はアーカイブのみできればよいので、圧縮方法に非圧縮を指定している

以下Wikipediaから一部改変して引用する

#### ローカルファイルヘッダ

|オフセット|サイズ|内容 ( = 今回の要件を満たす値 )|
|-|-|-|
|0|4|`PK\3\4`=0x504B0304|
|4|2|展開に必要なバージョン ( = v1.0(非圧縮) = 10 )|
|6|2|汎用目的のビットフラグ ( = 0(特になし) )|
|8|2|圧縮メソッド ( = 0(非圧縮) )|
|10|4|ファイルの最終変更日時 MS-DOS date/time format|
|14|4|非圧縮データのCRC-32|
|18|4|圧縮サイズ|
|22|4|非圧縮サイズ|
|26|2|ファイル名の長さ (n)|
|28|2|拡張フィールドの長さ (m) ( = 0 )|
|30|n|ファイル名|
|30+n|m|拡張フィールド ( = empty )|

#### セントラルディレクトリレコード

|オフセット|サイズ|内容 ( = 今回の要件を満たす値 )|
|-|-|-|
|0|4|`PK\1\2`=0x504B0102|
|4|2|作成されたバージョン ( = v1.0(非圧縮) = 0d10 )|
|6|2|> 展開に必要なバージョン|
|8|2|> |
|10|2|> |
|12|4|> `ローカルファイルヘッダ`|
|16|4|> `4~29 と同じ`|
|20|4|> |
|24|4|> |
|28|2|> |
|30|2|> 拡張フィールドの長さ (m)|
|32|2|ファイルコメントの長さ (k) ( = 0 )|
|34|2|ファイルが開始するディスク番号 ( = 0 )|
|36|2|内部ファイル属性 ( = 0 )|
|38|4|外部ファイル属性 ( = 0 )|
|42|4|ローカルファイルヘッダの相対オフセット|
|46|n|> ファイル名|
|46+n|m|> 拡張フィールド|
|46+n+m|k|ファイルコメント ( = empty )|

#### セントラルディレクトリ終端レコード

|オフセット|サイズ|内容 ( = 今回の要件を満たす値 )|
|-|-|-|
|0|4|`PK\5\6`=0x504B0506|
|4|2|このディスクの数 ( = 0 )|
|6|2|セントラルディレクトリが開始するディスク ( = 0 )|
|8|2|このディスク上のセントラルディレクトリレコードの数|
|10|2|セントラルディレクトリレコードの合計数|
|12|4|セントラルディレクトリのサイズ (バイト)|
|16|4|セントラルディレクトリの開始位置のオフセット|
|20|2|ZIPファイルのコメントの長さ (n) ( = 0 )|
|22|n|ZIPファイルのコメント ( = empty )|

### MS-DOS date/time format

MS-DOS date/time formatは、ローカルファイルヘッダとセントラルディレクトリレコードにおいて日時の表現に用いられる形式である

この名前で調べればMicrosoftのリファレンスが出てくる

タイムゾーンの概念がないことと解像度が2秒であることが特徴である

まずビッグエンディアンで考える

`YYYYYY`=年-1980 `MMMM`=月 `DDDDD`=日 `hhhhh`=時 `mmmmmm`=分 `sssss`=秒/2

とするとMS-DOS date/time formatは

```text
MSB <-- --> LSB
YYYYYYYM MMMDDDDD hhhhhmmm mmmsssss
```

これをリトルエンディアンに直すと

```text
MSB <-- --> LSB
mmmsssss hhhhhmmm MMMDDDDD YYYYYYYM
```

JSで実装すると

```js
const d=new Date(); // 注意: new Date().getMonth()は0~11

console.log(
  ((d.getFullYear()-1980)<<25)|
  ((d.getMonth()+1)<<21)|
  (d.getDate()<<16)|
  (d.getHours()<<11)|
  (d.getMinutes()<<5)|
  (d.getSeconds()>>1)
);
```

### CRC-32

CRC-32は、データ破損の検出に用いられる32bitのデータである

解凍ソフトは、ZIPファイルから得られたデータとヘッダに記録されているCRC-32を比較して、データが破損していないことを確認する

ZIPファイルを生成する場合はたとえ非圧縮であってもCRC-32を計算してファイルに含める必要がある

## 実物を用いた実験

7-ZipでZIPファイルを作成してバイナリエディタで観察した

`folder`ディレクトリに`aiu\neo`を書いた`a.txt`を入れて圧縮した

元データが十分に小さいため圧縮が行われなかった模様

```text
50 4b 03 04 // PK0304
14 00 // v2.0
00 00 // no flag
00 00 // cps stored
4b 6a 8e 55 // date
00 00 00 00 // crc32
00 00 00 00 // cps size
00 00 00 00 // raw size
07 00 // name l
00 00 // ex l
66 6f 6c 64 65 72 2f // name "folder/"

50 4b 03 04
0a 00 // v1.0
00 00
00 00
4f 6a 8e 55
33 5e b9 72
06 00 00 00
06 00 00 00
0c 00
00 00
66 6f 6c 64 65 72 2f 61 2e 74 78 74 // name "folder/a.txt"
61 69 75 0a 65 6f // content "aiu\neo"

50 4b 01 02 // PK0102
3f 00 // v created
14 00 // v required
00 00 // no flag
00 00 // cps stored
4b 6a 8e 55 // date
00 00 00 00 // crc32
00 00 00 00 // cps size
00 00 00 00 // raw size
07 00 // name l
00 00 // ex l
00 00 // cmt l
00 00 // disk start no
00 00 // in attr
10 00 00 00 // out attr
00 00 00 00 // rpos
66 6f 6c 64 65 72 2f // name "folder/"


50 4b 01 02
3f 00
0a 00
00 00
00 00
4f 6a 8e 55
33 5e b9 72
06 00 00 00
06 00 00 00
0c 00
00 00
00 00
00 00
00 00
20 00 00 00
25 00 00 00
66 6f 6c 64 65 72 2f 61 2e 74 78 74 // name "folder/a.txt"

50 4b 05 06 // PK0506
00 00 // disk no this
00 00 // disk no 0304 starts
02 00 // cnt 0102 on this disk
02 00 // cnt 0102 all
6f 00 00 00 // 0102 size
55 00 00 00 // 0102 rpos
00 00 // cmt l
```

一つ目のファイルエントリがディレクトリになっているが、これはv2.0からはファイルエントリにディレクトリも含めることができるためである

理解の確認のため以下を試みた

- ディレクトリのファイルエントリの削除
  - 対応するセントラルディレクトリレコードの削除
  - セントラルディレクトリレコードの編集
    - ローカルファイルヘッダの相対オフセット
  - セントラルディレクトリ終端レコードの編集
    - セントラルディレクトリレコードの数
    - セントラルディレクトリレコードのサイズ
    - セントラルディレクトリレコードのオフセット

以下のJSを用いて保存したZIPファイルが正常に展開できることを確認した

```js
Object.assign(
  document.createElement('a'),
  {
    download:'test.zip',
    href:URL.createObjectURL(
      new Blob([
        new Uint8Array(`

50 4b 03 04
0a 00 // v1.0
00 00
00 00
4f 6a 8e 55
33 5e b9 72
06 00 00 00
06 00 00 00
0c 00
00 00
66 6f 6c 64 65 72 2f 61 2e 74 78 74 // name "folder/a.txt"
61 69 75 0a 65 6f // content "aiu\neo"

50 4b 01 02
3f 00
0a 00
00 00
00 00
4f 6a 8e 55
33 5e b9 72
06 00 00 00
06 00 00 00
0c 00
00 00
00 00
00 00
00 00
20 00 00 00
00 00 00 00
66 6f 6c 64 65 72 2f 61 2e 74 78 74 // name "folder/a.txt"

50 4b 05 06 // PK0506
00 00 // disk no this
00 00 // disk no 0304 starts
01 00 // cnt 0102 on this disk
01 00 // cnt 0102 all
3a 00 00 00 // 0102 size 再計算
30 00 00 00 // 0102 rpos 再計算
00 00 // cmt l

        `.match(/\b[\da-f]{2}\b/g).map(x=>parseInt(x,16))
        ).buffer
      ])
    )
  }
).click();
```

## JSで実装

### TypedArray

### 非同期reduce

### MS-DOS date/time format

### CRC-32

## 性能

## 余談

### HTMLで複数ファイルの読み込み

複数ファイルをJSで読み込みたい場合は`<input type="file" webkitdirectory>`を使う

標準仕様として存在しているが名前にwebkitがついている

### "zip"という言葉

ZIPの制作者がzipという名前を自由に使って良いと表明したらしい

### deflateとzlibとgzipとZIPの関係

- deflate  
  可逆圧縮アルゴリズム
  - ZIP
    deflateを用いた圧縮が可能なアーカイブファイル形式
  - zlib  
    deflateを実装したライブラリ兼ヘッダー/フッターの定義
    - PNG  
      可逆圧縮可能な画像形式  
      データの圧縮にzlib形式を使用
  - gzip  
    deflateを用いた圧縮ファイル形式兼ヘッダー/フッターの定義

### 圧縮アルゴリズムの特許問題

幾つかの圧縮法は特許を取っている企業が存在した

GIF画像が使えなくなった騒ぎもこれである

zlibでの実装は特許に抵触しないように注意深く実装されたらしい

現在これらの特許は有効期限切れで気にする必要はない

### 自己解凍形式について

セントラルディレクトリ終端レコードに記録される「セントラルディレクトリのオフセット」と「ZIPファイルのコメントの長さ」を利用すると、
ZIPファイルの始端と終端に任意のデータを付加することができる

ここに解凍ソフトを入れて拡張子をexeにしたものが所謂自己解凍exeである

つまりファイルの始端又は終端に任意のデータを付加可能な形式であれば、理論上どんな形式でも自己解凍プログラムを作成可能である

ちなみに今回の延長で自己解凍HTML(非圧縮)を作ってみたが、場合によってZIP部が文書に表示されてしまっていまいちだった

## 参考文献

<https://ja.wikipedia.org/wiki/ZIP_(ファイルフォーマット)>

<https://gist.github.com/ysakasin/2edf8d3bf55c6ebf63f82851e302b030>

<https://learn.microsoft.com/en-us/windows/win32/api/oleauto/nf-oleauto-dosdatetimetovarianttime>
