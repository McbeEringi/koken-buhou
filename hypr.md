# Linuxに入門、Hyprlandが快適

22 McbeEringi

---

## 動機

2023年8月にCHUWI Minibook X N100を衝動買いした。
11インチ弱の小型ノートPCである。少し大きなUMPCといったところだろうか。

名前の通りCPUがIntel N100であり、RAMは12GB、ROMが512GB(nvme1.4)、デフォルトのOSはWindows11である。
Intel N100はCeleronの系譜を踏むエントリークラスのCPUであり、TDPが6Wと省電力でありながら従来の廉価CPUとは一線を画す性能で話題となった。
実際にWindows11でもブラウジングや軽度な開発程度であれば全く問題なく作業が可能である。


このPCの弱点はその電池持ちである。
公称8時間であるが実際の駆動時間は6時間程度であり、メインのノートPCとして用いるには少々心もとない。


*小さいノートPCを買ったのはいいがモバイルバッテリーが欠かせない*


そんな悩みを抱えていた時、とある部員からArch Linuxを勧められその沼へ足を踏み入れる運びとなったのだった……

## Arch Linux

LinuxはフリーなOSであるため、多数の派生OS = ディストリビューションが存在する。
Arch Linuxはその中でも比較的新しく、軽量かつ柔軟、シンプルなディストリビューションである。
統計によると2024年3月現在の工学研究部では約1/3程度の部員がArchLinuxを使用している(憶測)。
Arch Linuxのメリットとして例えば

- Arch Linux Users Repositoryにほしいソフトウェアはだいたい揃っている
- 最小限の機能のみであるため望まない機能を入れないことが容易
- Linux界隈最大級のwiki、ArchWikiがあるため情報が豊富

等が挙げられる。

私はLinuxの経験が殆ど無く、触ったことがあるのは精々授業でのUbuntuと、iOSのエミュレータアプリ「iSH」で動くAlpine Linux程度であった。
最近のUbuntuやDebianはGUIによるインストールが一般的であるが、Arch Linuxのインストールではコマンドの操作が必須である。
しかしArch LinuxにはLinux界隈でも最大規模のWiki、ArchWikiがついているのでこれを参考にインストールを進めた。
今回はWindows11とArch Linuxのデュアルブート環境を構築した。

Arch Linux本体のインストールについては多数の解説が存在するのでここでは割愛、要点のみまとめる。

- パーティション
	- nvme0n1p1 EFI 100M
	- nvme0n1p2 Windows 予約領域 128M
	- nvme0n1p3 Windows root 125G
	- nvme0n1p4 Linux EFI  550M systemd-boot
	- nvme0n1p5 Linux swap 16G ext4
	- nvme0n1p6 Linux root 333.5G
	- nvme0n1p7 Window回復領域 1.7G
- 無線: iwd + systemd-resolved
- AUR: yay

## デスクトップ環境? ウィンドウマネージャ??

Linuxはそれ単体ではGUIを持ち合わせておらず、一般にはデスクトップ環境(Desktop Environment = DE)を併せてインストールする。
よく知られているのがUbuntu標準であるGNOME、Windowsに似た操作感であるKDE、軽量なXfceなどである。

デスクトップ環境は、GUIの描画とウィンドウの管理を行うウィンドウマネージャ(Window Manager = WM)と、ターミナルエミュレータやファイルマネージャ等の実用上重要なアプリケーションの集合である。

ウィンドウマネージャは操作体型によりスタック型とタイル型に大別される。

スタック型では、ウィンドウを机の上の紙のように移動し重ねることができる。
今日一般に広く普及している操作体型である。
GNOME(のWMであるMutter)やKDE(KWin)、Xfce(Xfwm)はこちらに分類される。

タイル型では、ウィンドウは自動的に敷き詰められ基本的に重なることがない。
これによりウィンドウをレイアウトする作業から開放されると同時に画面空間を最大限活用できるようになる。
今回扱うHyprlandの他にsway、awesome、i3等がこちらに分類される。

ウィンドウマネージャとLinux本体の通信プロトコルは一般にX Window Systemが用いられるが、ここ数年の間に後継となるWaylandが勢力を伸ばしている。
大手のウィンドウマネージャは殆どWaylandに対応している。

以上の話をまとめると以下の通り。

||X Window System|Wayland|
|-|-|-|
|タイル型|awesome,i3|**Hyprland**,sway|
|スタック型|Muter,KWin,Xfwm|Mutter,KWin|

## Hyprland

HyprlandはWaylandで動くタイル型のウィンドウマネージャである。
アプリケーションの起動及び終了時のアニメーションやウィンドウ背景のブラー効果、記述しやすい設定ファイルなどが特徴である。
もちろん仮想デスクトップやトラックパッドによる仮想デスクトップ間の移動にも対応している。

同じくWayland対応のタイル型ウィンドウマネージャであるswayに対して特に視覚効果が豪華である。
初めてMacを触ったときと似たような感動が味わえる。


- タイル型としては視覚効果が豪華
	-
- 設定ファイルが理解しやすい

タイル型は
キーバインドを覚えなきゃ→トラックパッドユーザーでも超快適



## おすすめアプリ集

- **GIMP** 画像編集ソフト
- **Firefox** ブラウザ
- **mpv** 軽量メディアプレーヤ
- **OpenSCAD** 簡便な3DCAD
- **ImHex** バイナリエディタ



## 入れてよかったこと

- 電池持ちが良い 体感1.5倍
- CPU,RAMがスカスカ RAM食って4GB
- マイコンのコンパイルが速い
- GIMPの起動が爆速 もはやビュアー
- KiCADにダークモードがある